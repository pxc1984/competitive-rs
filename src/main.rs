#![allow(dead_code)]
#![allow(unused)]
mod add_two_numbers;
mod longest_substring_without_repeating_characters;
mod two_sum;

fn main() {
    panic!("This crate is supposed to be run with cargo test");
}
